workflow: low_snr_restoration
data:
  source: Paired low-SNR microscopy crop and clean reference
  source_urls:
    - user-supplied microscopy restoration data
  license: Use the terms for the supplied restoration data
  citation: CAREamics / Noise2Void / CARE-style restoration evaluation workflow
  normal_ci: supplied clean/degraded microscopy crop pair
  public_data: optional paired low-SNR public restoration crop
  markers:
    - model_runtime
  selected_files:
    - low_snr_clean_crop.tif
    - low_snr_degraded_crop.tif
    - careamics_checkpoint.ckpt
  expected_layout: YX
  expected_dtype: float32
