workflow: cell_counting_phenotyping
outputs:
  terminal_node: summarize_phenotypes
  required_columns:
    - image
    - object_count
    - mean_area
    - total_area
    - mean_centroid_y
    - mean_centroid_x
    - mean_intensity
    - mean_perimeter
    - mean_equivalent_diameter
  required_nodes:
    - segment_cells
    - measure_regions
    - measure_shape_features
    - measure_intensity_features
    - region_shape_table
    - object_feature_table
    - summarize_phenotypes
  deterministic_acceptance:
    row_count: 1
    min_object_count: 1
