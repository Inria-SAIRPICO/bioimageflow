[project]
name = "bioimageflow"
version = "0.1.5"
description = "Python library for orchestrating bioimage analysis workflows"
requires-python = ">=3.10"
dependencies = [
    "bioimageflow-core>=0.1.5",
    "pandas",
    "pyarrow",
    "pydantic",
    "wetlands>=1.0.8",
]

[tool.uv.sources]
bioimageflow-core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
