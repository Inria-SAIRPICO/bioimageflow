[project]
name = "bioimageflow-segmentation-tools"
version = "0.1.6"
description = "Segmentation tools for BioImageFlow"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "bioimageflow-core>=0.1.6",
    "imageio",
    "numpy",
    "scikit-image",
]

[tool.uv.sources]
bioimageflow-core = { workspace = true }
bioimageflow = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
