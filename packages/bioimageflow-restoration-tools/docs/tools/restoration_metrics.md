# RestorationMetrics

`RestorationMetrics` compares clean, degraded, and restored images.

It reports MSE, PSNR, and residual-noise estimates so restoration workflows can assert numeric improvement on pinned fixtures.
