[project]
name = "bioimageflow-core"
version = "0.1.6"
description = "Worker-safe core types and tool base classes for BioImageFlow"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
